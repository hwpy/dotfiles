#!/bin/sh
# Chromium: определяет платформу по переменным окружения
if [ -n "$WAYLAND_DISPLAY" ]; then
    exec /usr/bin/chromium --ozone-platform=wayland "$@"
elif [ -n "$DISPLAY" ]; then
    exec /usr/bin/chromium --ozone-platform=x11 "$@"
else
    exec /usr/bin/chromium "$@"
fi